import {StyleSheet} from 'react-native';

const FONT = StyleSheet.create({
  sectionTitle: {
    fontSize: 18,
    fontWeight: 'bold',
    marginHorizontal: 10,
    marginBottom: 10,
  },
});

const LAYOUT = StyleSheet.create({
  sectionTitle: {},
});

export {FONT, LAYOUT};
